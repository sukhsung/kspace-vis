


b = 2.9409;

theta = linspace(0,30,100);

kz = b*tand(theta);

lambda = 3.346;
N = 1;

num = 1-cos(2*kz*lambda*N);
den = 1-cos(2*kz*lambda);

y = num./den ;

yInt = 


figure
subplot(1,3,1)
plot(theta,num);
subplot(1,3,2)
plot(theta,den);
subplot(1,3,3)
plot(theta,y);